certifi==2026.7.22
charset-normalizer==3.4.9
beautifulsoup4>=4.12
idna==3.18
PySide6==6.11.1
PySide6_Addons==6.11.1
PySide6_Essentials==6.11.1
requests==2.34.2
shiboken6==6.11.1
urllib3==2.7.0

# the current track, one backend per platform — pip skips whichever does
# not match, so the same file installs on either
dbus-next==0.2.3; sys_platform == "linux"
winrt-Windows.Media.Control>=2.0; sys_platform == "win32"
winrt-Windows.Foundation>=2.0; sys_platform == "win32"
